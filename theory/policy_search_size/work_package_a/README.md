# Work Package A

Run from the repository root:

```bash
python3 theory/policy_search_size/work_package_a/verify.py
```

Default settings use 1,000,000 Monte Carlo states and seed 20260802.
Generated files are written to `results/`.
