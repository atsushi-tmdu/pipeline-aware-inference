# D8-A Scientific Run Pending

The generalized theory, design, and implementation locks are complete.

Scientific execution is authorized, but the registered scientific simulation
has not yet been run.

The next checkpoint must prepare the immutable run command, output directory,
seed inventory, resume/atomic-write contract, and final pre-run verifier
without changing the locked design or acceptance criteria.
