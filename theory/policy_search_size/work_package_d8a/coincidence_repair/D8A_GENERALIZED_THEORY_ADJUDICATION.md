# D8-A Generalized Coincidence Theory Adjudication

## Established

Under the stated winner-cell kernel regularity and strict
candidate-trigger separation:

- the generic moving-\(\max\) cell has a uniform piecewise quadratic
  expansion;
- the only active candidate coincidence hyperplanes are
  \(q_0=q_2\) and \(q_1=q_2\);
- the complete policy contrast has a finite-cone generalized second-order
  expansion;
- the reference expectation coefficient includes closed-form Gaussian
  positive-part-square corrections;
- the finite-\(n\) policy identity combines with the repaired reference
  coefficient;
- the TESS reference coefficient follows by the generalized smooth
  composition rule.

## Corrected status

The ordinary \(C^2\) theorem is not restored at coincidence. It is replaced
there by a stronger piecewise quadratic statement suitable for expectation
expansion.

## Numerical design

The historical design remains blocked until:

1. generalized oracle coefficients are implemented;
2. the 75-cell registry is checked against the repaired assumptions;
3. acceptance criteria are re-adjudicated;
4. a new design-repair and implementation lock is created.

Scientific simulation has not been run.
